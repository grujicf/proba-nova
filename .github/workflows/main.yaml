name: PR workflow - MAIN

on:
  pull_request:
    branches: main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout step
        run: echo "Hi from the main PR workflow"
      - uses: actions/checkout@v4.2.2
      - name: ls
        run: |
          ls
